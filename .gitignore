# ── Large models — hosted on GitHub Releases (see MODELS.md), not committed (>100 MB) ──
Resources/Models/Llama/*.gguf
Resources/KokoroSherpa/model.onnx
Resources/Supertonic/onnx/vector_estimator.onnx
Resources/Models/Embed/*.gguf
bin/WhisperServer/*.bin

# ── Runtime state ──
NPC_Memories.xml
NPC_PendingMemories_*.xml
*.log

# ── Build artifacts ──
*.pdb
obj/
.vs/

# ── OS ──
Thumbs.db
.DS_Store

# NOTE: bin/, Resources/, Config/, Plugins/ and the sidecar .exe/.dll ARE committed
# (everything <100 MB). Only the five model files above are fetched separately.
